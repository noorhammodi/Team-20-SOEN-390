{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cross-env NODE_ENV=production node ./bin/www",
    "dev"  : "cross-env NODE_ENV=development nodemon -L ./bin/www",
    "test" : "cross-env NODE_ENV=test jest --verbose --runInBand"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "debug": "~2.6.9",
    "dotenv": "^16.0.0",
    "express": "~4.16.1",
    "mongoose": "^6.1.8",
    "morgan": "~1.9.1"
  },
  "devDependencies": {
    "jest": "^27.5.0",
    "nodemon": "^2.0.15",
    "supertest": "^6.2.2"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
