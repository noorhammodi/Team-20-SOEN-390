version: "3.8"
services:
  express-app:
    build: ./backend/
    ports:
      - "3001:3001"
    environment:
      - PORT=3001

  react-app:
    build: ./frontend/
    ports:
      - "3000:3000"
    environment:
      - PORT=3000

volumes:
  backend-node:
  frontend-node: